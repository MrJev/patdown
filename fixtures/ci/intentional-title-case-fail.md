Temporary fixture for PR #5. Delete after line annotations are verified in Actions.

# The Complete Guide To Fuzzy Rules

This heading is deliberate title case so the "No title case" rule fails and the FAIL-only evidence Choice can pin a real line range in the GitHub annotation.
