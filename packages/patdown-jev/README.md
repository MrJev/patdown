# @patdown/jev

Effect HttpClient for TypeSafe System One (Jev). Used by the default [patdown](https://github.com/tyler-dot-earth/patdown) judge.

Install the CLI as `patdown`. This package is a library, not a command.
