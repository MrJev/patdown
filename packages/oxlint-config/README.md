# @patdown/oxlint-config

Shared Oxlint `baseConfig`, vendored anti-slop, and `patdown-contracts` package/vitest rules.

Contract rules run on each package's `oxlint.config.ts` and inspect sibling `package.json` / tsconfig / vitest files.
